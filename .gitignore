node_modules
dist
dev-dist

# Variables d'environnement (ne jamais commiter)
.env
.env.local
.env.work
.env.main

# Fichiers backup
*.backup
*.backup-*
*.OLD-*

# Artefacts debug TypeScript
tsc_errors.txt
tsc_errors_full.txt
tsc_output.txt

# Playwright : artefacts de tests (ne pas commiter)
playwright-report/
playwright-visual-report/
test-results/
e2e/.auth/
# Les snapshots visuels (e2e/snapshots/) sont intentionnellement commités comme baselines
